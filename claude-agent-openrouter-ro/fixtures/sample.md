# Sample snippet

Please translate the following codeblock into Romanian:

```python
# Greet the user by name
def greet(name):
    """Return a friendly welcome message."""
    # Build the message and return it
    message = f"Hello, {name}! Welcome to the workshop."
    return message


# Entry point for the demo script
if __name__ == "__main__":
    print(greet("Alex"))
```
